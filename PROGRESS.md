# Progress Log

> Append-only session log. Each session adds an entry at the bottom.
> Never edit previous entries.

<!-- Entry format:

## YYYY-MM-DD HH:MM -- [T-XX-N] Brief Title
- **What I did**: 1-3 sentences on concrete actions taken
- **Deliverables**: List of files created/modified
- **Sanity check result**: What I verified and the outcome
- **Status**: [DONE] Done / [PARTIAL] Partial (what remains) / [BLOCKED] Blocked (why)
- **Request**: Cross off TASK-XXX / Move TASK-XXX to In Progress / No change

-->

> Older entries archived to [archive/progress_log.md](archive/progress_log.md).
> 170+ session entries archived as of 2026-04-11.

- **What I did**: Redesigned RecipeCard to be compact (reduced padding, smaller text, single-row name+badge layout, truncated names). Added category grouping in BakingStudio browse mode -- recipes are now grouped under section headers (Base/Cream/Decoration/Complete) with descriptive captions. Removed unused category pill from cards since category is now shown via section headers. Maintained per-cake-type color themes.
- **Deliverables**: RecipeCard.tsx (compact layout), BakingStudio.tsx (category grouping with CATEGORY_SECTIONS + groupByCategory)
- **Sanity check result**: TypeScript type check passes, Vite build succeeds
- **Status**: [DONE]
> 213 session entries archived as of 2026-04-16.

## 2026-04-15 -- [T-P0-432] Staging 13-question flashcards for Google R1
- **What I did**: Compressed the 13-block mock interview diagnostic (staging/04_14_ML问题深入拷打.md) into Q/A flashcards. Each card has one self-check question and a concise answer (under 100 words) with at least one formula or numeric example. Cards cover: (1) Bias-Variance, (2) GBDT, (3) Tree vs NN tabular, (4) Class Imbalance, (5) ROC vs PR AUC, (6) Softmax stability, (7) Online Softmax rescale, (8) Batch Norm, (9) BN+Dropout variance shift, (10) SGNS/word2vec, (11) InfoNCE/contrastive, (12) Adam/AdamW, (13) NaN debugging. Ends with 3 mnemonics, 12-item pre-interview checklist (30-min pass), and 3 answer habits. Built with StudyNoteBuilder, ingested into company_documents (company_id=3, doc_id=57).
- **Deliverables**: scripts/seed_google_staging_13_flashcards.py, docs/google_staging_13_flashcards.md (12123 chars), DB record id=57
- **Sanity check result**: 1092 tests pass, ruff lint clean, DB record verified (company_id=3, source_type=manual)
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-432 --status completed`
## 2026-04-15 -- [T-P0-434] LC 85 Maximal Rectangle notes and completion
- **What I did**: Wrote comprehensive Chinese notes for LC 85 (Maximal Rectangle). Notes cover: (1) row-to-histogram transformation intuition with worked example, (2) monotonic stack solution (reusing LC 84) with sentinel trick, (3) why total complexity is O(mn) not worse, (4) DP three-array alternative (left/right/height), (5) comparison table of stack vs DP approaches, (6) 45-second interview pitch. Updated DB record to is_completed=1.
- **Deliverables**: scripts/_update_lc85_notes.py, DB record id=242 updated (notes_len=3798, is_completed=1)
- **Sanity check result**: 1092 tests pass, ruff lint clean, DB record verified
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-434 --status completed`
## 2026-04-15 -- [T-P0-435] K-largest heap/quickselect family drill: LC 703 + 973 + 378
- **What I did**: Wrote comprehensive Chinese notes for three K-largest family problems. LC 703 (Kth Largest in Stream): min-heap size k template, add() O(log k), plus a K-largest family comparison section (heap vs quickselect vs bucket sort with decision tree). LC 973 (K Closest Points): dual solution with max-heap O(n log k) and quickselect average O(n), complexity comparison table. LC 378 (Kth Smallest in Sorted Matrix): value-domain binary search with O(n) staircase counting from bottom-left, connection to LC 410 family. All three marked is_completed=1.
- **Deliverables**: scripts/_update_klargest_family_notes.py, DB records id=587 (notes_len=2775), id=150 (notes_len=2392), id=434 (notes_len=2002) all updated
- **Sanity check result**: 1092 tests pass, ruff lint clean on new script, DB records verified
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-435 --status completed`
## 2026-04-15 -- [T-P0-436] Pinterest Sketch/Streaming theory 1-pager
- **What I did**: Created a comprehensive 1-pager covering 4 streaming algorithms for Pinterest prep: (1) Count-Min Sketch with error bounds and overestimate-only property, (2) Space-Saving/Misra-Gries with deterministic heavy-hitter guarantees and CMS comparison, (3) Reservoir Sampling (LC 382/398) with weighted variant and Pinterest ad sampling application, (4) HyperLogLog for cardinality estimation with register math. Includes comparison table and interview bridge section for "real-time top-K trending pins" question (naive heap -> CMS+heap -> distributed sharding -> time decay).
- **Deliverables**: scripts/_seed_pinterest_sketch_streaming.py, docs/pinterest_sketch_streaming_1pager.md (6152 chars), DB company_documents id=58 (company_id=29)
- **Sanity check result**: 1092 tests pass, ruff lint clean, DB record verified
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-436 --status completed`
## 2026-04-15 -- [T-P0-429] Google R2 G&L top-20 BQ mapping audit
- **What I did**: Mapped all 20 common G&L behavioral questions to bq_improved_stories (EX-01 through EX-24, EX-33, COL-1 through COL-4). Each question has 2-3 story options. Identified EX-01/EX-17/EX-14/EX-20/EX-23 as versatile anchor stories. Confirmed EX-02/EX-08/EX-17 are already polished. No coverage gaps found -- all 20 questions covered. Noted 3 areas that could use a third story variant (deadline pressure, ethical choice, peer feedback).
- **Deliverables**: docs/bq_todo_tracker.md (appended top-20 mapping table + gap analysis section)
- **Sanity check result**: 1092 tests pass, ruff lint clean on changed file
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-429 --status completed`
## 2026-04-15 -- [code review] LC 85 user solution — boundary cleanup
- **What I did**: User finished LC 85 on their own and asked for a boundary/edge-case cleanup while preserving code style. Reviewed their monotonic-stack-on-histograms solution and applied 3 minimal changes: (a) added empty-matrix guard (`matrix=[]` and `matrix=[[]]` would IndexError on `matrix[0]`), (b) simplified the heights DP recurrence by dropping the redundant `heights[i-1][j] > 0` check (X+0 == X makes it noise), (c) renamed inner `n` to `L` inside `solve()` to avoid shadowing the outer `n`. Kept all their naming (`queue`/`ret`/`curIdx`/`curArea`), the two-stage init style, and the commented-out debug prints.
- **Deliverables**: Code-review reply on Discord (no repo changes — user holds the canonical solution).
- **Sanity check result**: Ran refactored code on 6 LC 85 test cases incl. the two empty-matrix edges — all pass (6, 0, 1, 0, 0, 4 vs expected).
- **Status**: [DONE]
- **Request**: T-P0-434 already completed in prior autonomous session; no task update needed.
## 2026-04-15 -- [fix] StudyNoteBuilder auto-bold bleeding into LaTeX / code
- **What I did**: User reported markdown rendering issues in framework node 195 (http://localhost:5173/framework/195/notes). Traced to `StudyNoteBuilder._auto_bold_terms` applying regex substitution over the entire document without excluding math/code spans, despite the docstring claiming it did. Any registered term (MSE, VRM, CMS, etc.) whose first occurrence landed inside `$$...$$` or `$...$` got wrapped as literal `\mathrm{**MSE**}`, which renders as asterisks-in-math rather than bold. Rewrote the method to split content on protected spans (fenced code, inline code, display math, inline math) and only substitute inside prose, stopping after the first hit. Re-seeded node 195 via seed_bias_variance_geometric.py; direct UPDATE on doc 55 since save_to_db is insert-skip-idempotent. Regenerated docs/google_regularization_deep_dive.md.
- **Deliverables**: scripts/study_note_builder.py (rewritten _auto_bold_terms), tests/test_study_note_builder.py (+2 regression tests), docs/google_regularization_deep_dive.md (clean re-gen), DB: node 195 content updated, doc 55 content patched. Committed as 9e9a05d, pushed to main.
- **Sanity check result**: 1094 tests pass (+2 new), ruff clean, emoji scan clean. Post-fix scan: 0 `\mathrm{...**...}` or `\text{...**...}` hits in any node or the 4 Google prep docs. Remaining "bold-in-math" regex hits in docs 45/54 (DoorDash) are a different pre-existing bug unrelated to this fix (display-math delimiters used as prose brackets) — user-unreported, separate scope.
- **Status**: [DONE]
- **Request**: No task change (Q&A-triggered fix, not tied to an open task).
## 2026-04-15 -- [fix] DoorDash Price-Tier dollar signs swallowed by math tokenizer
- **What I did**: Scanned all DoorDash docs after the earlier `** in math` finding. Most hits my scanner flagged turned out to be false positives (legitimate `$$...$$` math closers followed by prose lists). The only real bug was line 214 of `docs/doordash_ml_domain_llm_frontier.md`: `Level 3: Price Tier ($ / $$ / $$$ / $$$$)` — four consecutive dollar-token prices that a markdown+math renderer eagerly pairs into a malformed math span. Wrapped each tier in backtick inline code: `(`$` / `$$` / `$$$` / `$$$$`)`. Re-seeded doc 45 via its seed script; patched doc 54 (Master aggregate, no seed source) with a direct UPDATE.
- **Deliverables**: `docs/doordash_ml_domain_llm_frontier.md` (1-line escape fix), DB docs id=45 and id=54 both updated. Committed as `0a22c37`, pushed.
- **Sanity check result**: 1094 tests pass, ruff clean, emoji clean. Post-fix: zero plain-prose `$$` ambiguity in DoorDash docs. The `$$ price range` on line 65 of the same md is inside a fenced code block and needs no fix.
- **Status**: [DONE]
- **Request**: No task change.
## 2026-04-15 -- [lc] Add LC 1135 Connecting Cities With Minimum Cost with bidirectional link to LC 815
- **What I did**: User shared their Kruskal MST solution for LC 1135 and asked to add it with a follow-up link to LC 815. LC 1135 was missing from the problems table; LC 815 already existed (id=217) with a "进阶追问" section that already listed "LC 1135 Connecting Cities" (so the 815->1135 forward reference pre-existed). Fetched description from leetcode.ca (per reference memory), wrote a 2.1 KB Chinese notes block covering Prim-vs-Kruskal tradeoffs, UF path-compression / union-by-rank invariants, early-termination at `target == 1`, common 1-indexed gotchas, and the follow-up bridge explaining why LC 815 is "weighted MST -> unweighted BFS" abstraction-wise. Wrote an idempotent seed script (handles both INSERT and UPDATE-to-fill-gaps paths; gracefully no-ops the problem_family_links linking since that table doesn't exist in this schema).
- **Deliverables**: `scripts/add_lc1135_mst_with_notes.py`; DB problem id=1087 created with url/description/notes/family='mst'/is_completed=1. Committed as `a46c2f0`, pushed.
- **Sanity check result**: 1094 tests pass, ruff clean, emoji clean. Ran seed twice — second run reports UPDATE with only {is_completed, family} fields touched (notes and description preserved), confirming idempotency.
- **Status**: [DONE]
- **Request**: No task change (user request, not tracked as a formal task).
## 2026-04-15 -- [lc] LC 1570 Dot Product of Two Sparse Vectors -- notes + mark complete
- **What I did**: User shared their hashmap solution for LC 1570. Problem already existed in DB (id=239) with URL and description but no notes and `is_completed=0`. LC 311 already referenced 1570 in its 进阶追问 section (user pre-existing), so the forward link was there. Wrote 2.5 KB Chinese notes covering: (A) hashmap solution with the "iterate the smaller side" optimization, (B) sorted (index, value) pairs + two-pointer alternative with tradeoff table, (C) the ~5-10% density switch-over where dense arrays beat sparse, (D) explicit callout of the `defaultdict` access-creates-entry trap and commutativity check, (E) the LC 311 follow-up bridge explaining "nnz small -> map; both sparse -> take key intersection and multiply". Idempotent seed script.
- **Deliverables**: `scripts/_update_lc1570_notes.py`; DB problem id=239 now has `is_completed=1`, `family='sparse_representation'`, `pattern='hash map'`, and the Chinese notes. Committed as `f9eac5a`, pushed.
- **Sanity check result**: 1094 tests pass, ruff clean, emoji clean. Second seed run only re-asserts `is_completed` (idempotent; notes/family/pattern untouched once set).
- **Status**: [DONE]
- **Request**: No task change.
## 2026-04-15 -- [lc] LC 1244 Approach A: surface the size-K min-heap mechanism
- **What I did**: User pushed back on the LC 1244 notes: Approach A was `return sum(heapq.nlargest(K, values))`, which reads as "I know the API" not "I know the algorithm". Rewrote Approach A to an explicit size-K min-heap with `heapreplace`, kept the `nlargest` one-liner as an inline comment for the "if you can use it" path. Added a paragraph explaining why the manual version is the interview default (shows understanding) and called out `heapreplace` vs `heappop + heappush` as a bonus keyword. Complexity and correctness unchanged: O(N log K) time, O(K) space.
- **Deliverables**: `scripts/_update_lc1244_approach_a.py` (idempotent: locates the old block by exact match, replaces only if present and not already rewritten); DB problem id=199 notes updated (5577 -> 6177 chars). Committed as `0c1d51e`, pushed.
- **Sanity check result**: 1094 tests pass, ruff clean, emoji clean. Second run reports [NOOP] as expected.
- **Status**: [DONE]
- **Request**: No task change.
## 2026-04-15 -- [pinterest] Doc 47 refactor: drop static prep scaffolding, unify problem tables
- **What I did**: User said the sections from "Core Patterns Cheat Sheet" through "Daily Review Template" are not useful ("都done了 written note了 我不需要这些静态字段") and asked to either fold or delete them, elevate the actual problem inventory (custom + expansion + follow-ups) into one coherent reading flow, and replace the redundant Status/Notes columns (every row read "Done / Written") with a Chinese 考察要点 column carrying real review signal. User also asked to merge the additional LC 1135 + 1570 (added earlier today) into the unified table rather than a separate trailing section. Wrote a new full-body version: dropped Pattern Clusters + Core Patterns Cheat Sheet + Common Traps + Daily Review Template + the dead Links section; merged 2025-11 expansion and today's 815/311 follow-ups into one "扩展 & Follow-up" table with a 来源 column; rewrote Quick Status into "核心 14 道" with 考察要点 per row; kept SD modules / BQ cross-ref / recruiter prep pointer for navigation.
- **Deliverables**: `scripts/_rewrite_pinterest_lc_doc47.py` (idempotent via "*Last refactored: 2026-04-15.*" marker); DB doc id=47 content rewritten 12701 -> 6492 chars (49% shrink, higher signal density). Committed as `73c0bf2`, pushed.
- **Sanity check result**: 1094 tests pass, ruff clean (fixed W605 by switching NEW_CONTENT to raw string since the markdown-escaped `\|` in `O(\|t\|·26)` was being treated as an invalid Python escape), emoji clean. Second run reports [NOOP] as expected.
- **Status**: [DONE]
- **Request**: No task change.
## 2026-04-15 -- [pinterest] Translate problem statements to Chinese for 7 custom + 6 LC follow-ups
- **What I did**: User asked to translate the 题面 for Pinterest's custom + 补全 problems into Chinese while preserving original meaning. Translated 13 problem descriptions: 7 Pinterest custom (Escape Room Game State id=1068, Lighthouse 2D id=1071, Prefix-Match First-Word-Index id=1072, round() from scratch id=1073, round_by_precision id=1074, Grant Access DAG id=1075, Pin Connectivity id=1076) and 6 recent LC follow-ups I added over the last two days (LC 1135 Connecting Cities, LC 85 Maximal Rectangle, LC 1570 Dot Product Sparse Vectors, LC 703 Kth Largest Stream, LC 973 K Closest Points, LC 378 Kth Smallest Sorted Matrix). API names, variable names, and formulas stay English; everything else Chinese.
- **Deliverables**: `scripts/_translate_pinterest_descriptions_to_zh.py` (idempotent via trailing marker `*题面中文翻译 2026-04-15，原英文见 git 历史。*`); 13 `problems.description` rows rewritten; `description_source` set to `zh-translation`. Committed as `c3ef731`, pushed.
- **Sanity check result**: 1094 tests pass, ruff clean, emoji clean. Second run reports `updated=0 skipped=13` (idempotent).
- **Status**: [DONE]
- **Request**: No task change.
## 2026-04-15 -- [T-P0-424] Slack (Salesforce) HR call prep doc
- **What I did**: Recruiter screen today 2026-04-15 Wed 14:00 EST = 11:00 PT, 30-45 min, Slack ML team under Salesforce ownership. Wrote a full HR-call prep note following the Uber HR call format: 90-sec self-intro (eBay Ranking-as-Allocation + LLM-as-Judge eval, tuned to Slack collaboration ML angle), why-Slack (collab ranking + search + summarization canvas + Slack AI/Agentforce thesis), Q2-Q7 high-frequency answers (why leaving, why Slack, ML project deep dive, comp expectation $400-550K TC, timeline 4-6 weeks, 3 prepared questions: team structure / ML roadmap / interview loop), Slack product-line ML context (Slack AI, message/channel ranking, enterprise search, Agentforce), red flags, post-call action items, and a one-line cheat-sheet table. Seeded the doc into `company_documents` (company_id=32), flipped `companies.status` to `phone_screen`, added `interview_stages`, and enriched the existing `interview_events` row for the 2 PM PT = 11 AM PT hr_call.
- **Deliverables**: `docs/slack_hr_call_prep.md` (new, ~270 lines); `scripts/seed_slack_hr_call_prep.py` (idempotent upsert by title). `company_documents` id=59 created; Slack company row (id=32) and interview_events row updated.
- **Sanity check result**: Ruff clean on seed script; emoji scan reports 0 hits on both files; second run of seed script reports `[UPDATE]` instead of `[INSERT]` (idempotent). DB reads back all three updates (doc, company row, event row).
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-424 --status completed`
## 2026-04-15 -- [T-P0-415] LambdaRank/LambdaMART derivation drill (Google R1)
- **What I did**: Built a targeted 2-min oral drill note for T-P0-415. Four sections mapped to AC: (1) RankNet pairwise loss L=log(1+exp(-(s_i-s_j))) with gradient -(1-P_ij); (2) LambdaRank lambda_ij = RankNet gradient * |deltaNDCG_ij| with per-doc aggregation, plus LambdaMART = (lambda_i, w_i) fed to GBDT; (3) pointwise BCE / pairwise RankNet / listwise ListNet / LambdaMART comparison table with when-to-use rules; (4) Sale NDCG -> GMB bidding story hook (eBay Ranking-as-Allocation, +1% GMB first A/B). Added 11-item self-check checklist. Used StudyNoteBuilder with FormulaBlock for all math; escaped literal \\$5/\\$100 so they don't render as inline math.
- **Deliverables**: `docs/google_lambdarank_drill.md` (new, ~135 lines); `scripts/seed_google_lambdarank_drill.py` (idempotent). `company_documents` id=60 inserted (company_id=3 Google).
- **Sanity check result**: Ruff clean on seed script; emoji scan = 0 hits on generated doc; seed re-run reports `[SKIP]` (idempotent); DB read-back confirms id=60, company_id=3, length=8910. StudyNoteBuilder's single-dollar check passed after escape.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-415 --status completed`
## 2026-04-15 -- [T-P0-416] NDCG/MAP/MRR + position bias drill (Google R1)
- **What I did**: Built the T-P0-416 drill note covering all four AC points: (1) from-memory DCG@K=sum(2^y_i-1)/log2(i+1) with NDCG=DCG/IDCG and the why-of-exponential-gain / why-of-log-discount rationale; (2) MAP with AP=(1/R)sum(Precision@k * 1[y_k relevant]) and a three-bullet explanation of why binarization destroys graded-label information (y=1 and y=4 collapse); (3) position bias via examination hypothesis P(click|q,d,k)=P(examine|k)*P(relevant|q,d), why it inflates offline NDCG for the production ranker, IPW and ULTR/PAL fixes with formulas; (4) numerical toy [3,2,1,0,0] vs [1,1,3,0,0] showing NDCG=1.000 vs 0.546 while MAP ties at 1.000. Included MRR + 4-row cheat-sheet table (NDCG/MAP/MRR/Precision@K) and an 11-item 2-min self-check checklist. Filed as companion to google_lambdarank_drill.md so R1 ranking topics sit side-by-side.
- **Deliverables**: `docs/google_ndcg_map_mrr_drill.md` (new, 11651 chars); `scripts/seed_google_ndcg_map_mrr_drill.py` (idempotent). `company_documents` id=61 inserted (company_id=3 Google).
- **Sanity check result**: Ruff clean on seed script; emoji scan = 0 hits on generated doc; orphan single-dollar scan = 0; seed re-run reports `[SKIP]` (idempotent); DB read-back confirms id=61, company_id=3, length=11651. StudyNoteBuilder passed its internal single-dollar check.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-416 --status completed`
## 2026-04-15 -- [T-P0-417] Calibration drill: Platt/Isotonic/Temperature + GMB bidding (Google R1)
- **What I did**: Built the T-P0-417 drill note covering all five AC points: (1) Platt = sigmoid(A*z+B) with two scalars fit by NLL on held-out set, plus when-it-works / when-it-fails; (2) Isotonic regression via PAV as a monotone step function, rank-preserving (AUC unchanged) but coarse and data-hungry; (3) Temperature scaling softmax(z/T) with the argmax-invariance proof -- single scalar fit on val NLL, the default deep-net fix (Guo 2017 ResNet ECE 15% -> 1%); (4) reliability diagram with bin-wise conf(B_m) vs acc(B_m) against y=x plus ECE=sum(|B_m|/N * |acc-conf|) with production target < 2%; (5) GMB bidding calibration trap -- bid = value * P(action) in second-price auctions, miscalibration factor k bakes directly into realized CPA (target_CPA/k), AUC is blind to this because ranking is preserved. Added 7-row Platt/Isotonic/Temperature comparison table (parameters, rank-preserving, granularity, min data, best-for, typical use, risk), a numerical toy with k=1.5 over-confidence yielding 50% CPA overspend, and a 10-item 2-min oral self-check. Escaped dollar amounts as "USD" to avoid orphan-single-dollar failure.
- **Deliverables**: `docs/google_calibration_drill.md` (new, 11631 chars); `scripts/seed_google_calibration_drill.py` (idempotent). `company_documents` id=62 inserted (company_id=3 Google).
- **Sanity check result**: Ruff clean on seed script; emoji scan = 0 hits on generated doc; orphan single-dollar scan = 0; seed re-run reports `[SKIP]` (idempotent); DB read-back confirms id=62, company_id=3, length=11631. StudyNoteBuilder's internal single-dollar check passed after USD substitution.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-417 --status completed`
## 2026-04-15 -- [T-P0-418] IPS / counterfactual eval / debiased NDCG drill (Google R1, SIGIR talking points)
- **What I did**: Built the T-P0-418 drill note covering all five AC points: (1) IPS reweighting c_i / p_i as the Horvitz-Thompson estimator of a counterfactual policy value under overlap, plus per-position form under PBM and propensity clipping pitfall; (2) examination hypothesis P(click|q,d,k)=P(exam|k)*P(rel|q,d) with PBM vs cascade contrast; (3) SNIPS = sum(w*c)/sum(w) self-normalization with bias^2/variance MSE decomposition and the full estimator ladder (naive -> IPS -> clipped -> SNIPS -> DR); (4) one-line SIGIR contribution + limitation template with concrete propensity-harvesting example + mobile-cascade PBM violation limit, graded 1-4 quality ladder; (5) propensity estimation four ways -- RandPair result-randomization with CTR ratio p_k/p_1, intervention harvesting from naturally varying production reranks, Regression-EM on PBM, and parametric p_k fit with p_1~1 anchor. Added 6-column IPS/SNIPS/DR comparison table (unbiasedness, variance, propensity/reward needs, failure modes, LTR use), debiased NDCG formula under PBM with 4 known failure modes, numerical sanity check on p_10=0.05 blowup and tau=0.1 clip mitigation, and a 10-item 2-min oral self-check.
- **Deliverables**: `docs/google_ips_counterfactual_drill.md` (new, 14681 chars); `scripts/seed_google_ips_counterfactual_drill.py` (idempotent); `scripts/_sanity_ips_drill.py` (one-off scan helper). `company_documents` id=63 inserted (company_id=3 Google).
- **Sanity check result**: Ruff clean on seed script; non-ASCII scan = 0 hits on generated doc; orphan single-dollar scan = 0; seed re-run reports `[SKIP]` (idempotent); DB read-back confirms id=63, company_id=3, length=14681. StudyNoteBuilder's internal single-dollar check passed.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-418 --status completed`
## 2026-04-15 -- [T-P0-419] Two-Tower retrieval deep-dive drill (Google R1, beyond InfoNCE)
- **What I did**: Built the T-P0-419 drill note covering all four AC points for Google R1 staging beyond staging-11's InfoNCE flashcard: (1) Why two towers -- dot-product factorization u^T v means v_phi(d) never sees q, so doc embeddings precompute offline and an ANN index gives sub-ms retrieval; cost is no cross-attention so cross-features must live in the reranker, hence the retrieve -> rerank pipeline; (2) Negative sampling four strategies with per-strategy failure modes -- random (too easy), in-batch with log p_j popularity correction (popularity bias), hard from snapshot (false-negative + self-reinforcing loop), mixed (blend 50/30/20), plus the cross-cutting positive-only selection bias that echoes T-P0-418 IPS eval; (3) HNSW vs IVF-PQ vs Flat 8-row comparison table (structure, build time, latency, memory, recall@100, insert support, distance metric, failure mode) with pick-when rules (HNSW <= 1e8 + tight SLA + incremental updates; IVF-PQ at 1e9+ with batch rebuild; Flat for debug / ground truth), plus over-retrieve-2-5x + tune-against-Flat guidance; (4) Five sources of offline recall@K vs online NDCG skew -- index recall vs Flat, feature freshness skew, reward misalignment (click vs graded), rerank inversion (calibration broken after retriever swap), popularity distribution drift -- with a targeted offline diagnostic per source, and the debiased-NDCG escape hatch. Added concrete numerical scales (d=64-256, HNSW ~640B/vec at d=128+M=16, PQ 8-16B/vec, tau=0.05-0.1) and an 11-item 2-min oral self-check.
- **Deliverables**: `docs/google_two_tower_retrieval_drill.md` (new, 17251 chars); `scripts/seed_google_two_tower_retrieval_drill.py` (idempotent). `company_documents` id=64 inserted (company_id=3 Google).
- **Sanity check result**: Ruff clean on seed script; non-ASCII scan = 0 hits on generated doc; orphan single-dollar scan = 0 (20 total $, 10 $$ pairs); seed re-run reports `[SKIP]` (idempotent); DB read-back confirms id=64, company_id=3, length=17251. StudyNoteBuilder's internal single-dollar check passed.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-419 --status completed`
## 2026-04-15 -- [T-P0-420] Multi-objective ranking drill: DPP/MMR + Etsy diversity (Google R1)
- **What I did**: Built the T-P0-420 drill note covering all AC points beyond staging-5's MTL-balancing flashcards: (1) MMR closed form score = lambda * rel - (1 - lambda) * max_sim, greedy O(k |C|), single-knob tunability, why industry ships it (latency, PM-ergonomics, similarity flexibility); (2) DPP via P(S) prop to det(L_S) with L_ii = q_i^2 and L_ij = q_i * phi_i . phi_j * q_j so one determinant encodes quality AND repulsion; greedy MAP O(k^2 |C|) via Cholesky update with (1 - 1/e) submodular guarantee; why DPP rarely ships (kernel design, latency, interpretability, stochastic sampling); (3) Intent collapse as the failure mode scalarization CANNOT fix regardless of w_k tuning -- minority intents have small training support, so GradNorm / Uncertainty Weighting do not help; platform fix = ALLOCATION PRIMITIVE (slot budgets + module arbitration) with per-module MMR inside, illustrated by Facebook / YouTube / Pinterest / DoorDash / Etsy examples; (4) 7-row orthogonality table (training-time MTL balancing vs serving-time slate diversity) on tools, what-it-balances, layer, per-request cost, failure modes each side fixes / does not fix, A/B surface; plus the slate-level non-decomposability argument that a diversity LOSS term cannot replace MMR; (5) Etsy GMB 2019-2021 war story -- title-cosine MMR diversified surface signal while seller concentration stayed at Gini 0.85; two-knob fix = composite similarity (title OR seller OR category) + slot-budget allocation for minority-intent modules; 90-day minority-intent CTR +5-8%. Added numerical scales (|C| ~ 200, k ~ 20, MMR 1-2ms, DPP 10-20ms, lambda 0.5-0.8, allocation 15-25%, slate entropy target >= 2.5 nats), plus an 11-item 2-min oral self-check. Escaped dollar amounts as 'N USD' to avoid orphan-single-dollar failure (same workaround as T-P0-417 calibration drill).
- **Deliverables**: `docs/google_dpp_mmr_diversity_drill.md` (new, 22011 chars); `scripts/seed_google_dpp_mmr_diversity_drill.py` (idempotent). `company_documents` id=65 inserted (company_id=3 Google).
- **Sanity check result**: Ruff clean on seed script; non-ASCII scan = 0 hits on generated doc; orphan single-dollar scan = 0 (16 total $, 4 $$ display-math pairs); seed re-run reports `[SKIP]` (idempotent); DB read-back confirms id=65, company_id=3, length=22011; all required section markers present (MMR, DPP, Intent Collapse, Allocation Primitive, GradNorm, Etsy, det(L_S)). StudyNoteBuilder's internal single-dollar check passed after USD substitution.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-420 --status completed`
## 2026-04-15 -- [T-P1-440] Pinterest card index: backend + data prep
- **What I did**: Extended `CompanyDocument.doc_kind` enum to include `card_index` (model + migration 19 string), added one-shot migration `scripts/_migrate_doc_kind_add_card_index.py` that copy-swaps `company_documents` to rewrite the CHECK constraint on existing DBs, tagged LC 85 (problems.id=242 Maximal Rectangle) with "Pinterest" in `company_tags`, and seeded a single `card_index` document for Pinterest (company_id=29) containing 10 cluster cards that group 29 problems. Verified 29 (not 28) — the task spec's AC text said 28 but the explicit per-card listing enumerated 29 distinct problem IDs, so I followed the listing.
- **Deliverables**: `src/backend/models/company.py` (CHECK), `src/backend/database.py` (migration 19 enum), `scripts/_migrate_doc_kind_add_card_index.py` (NEW), `scripts/_seed_pinterest_card_index.py` (NEW). DB: `company_documents` id=66 inserted (company_id=29 Pinterest, doc_kind=card_index); `problems.id=242` company_tags now `['LinkedIn','Uber','Adobe','Pinterest']`; CHECK constraint rewritten preserving all 55 pre-existing rows.
- **Sanity check result**: Ruff clean on all 4 changed files. Existing DB: migration ran once, re-run reports `[SKIP]` (idempotent). Seed re-run reports `[SKIP] LC 85 already tagged Pinterest` + `Updated card_index doc id=66` (upsert, no dup). Fresh in-memory DB smoke test: `card_index` accepted, bogus `doc_kind='bogus'` rejected with IntegrityError. AC checks all pass: (1) CHECK allows card_index, (2) LC 85 tagged Pinterest, (3) exactly 1 card_index doc, (4) JSON parses schema_version=1, (5) 10 cards with 29 problems, all IDs resolve, (6) seed is idempotent.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-440 --status completed`
## 2026-04-15 -- [T-P1-441] Pinterest card index: frontend CardGrid component
- **What I did**: Created `CompanyCardIndex.tsx` that fetches `GET /api/companies/:id/documents`, filters to `doc_kind === 'card_index'`, parses the JSON payload, and renders a responsive `grid-template-columns: repeat(auto-fit, minmax(380px, 1fr))` grid of bilingual cluster cards. Each card shows `name_zh` (bold) + ` -- name_en` (muted), an italic summary, and a problem list; cards with >5 problems collapse behind `<details>`. Problem rows render as buttons that invoke `onLcClick(leetcode_id)` for LC-tagged problems or `onDbClick(id)` for custom ones (leetcode_id=null). Extended `CompanyDocumentResponse` pydantic schema with `doc_kind: str = "prep_note"` so the field is actually serialized on the API side (previously missing; would have made frontend filter impossible). Added `CardIndexContent`, `CardIndexCard`, `CardIndexProblem`, `CompanyDocument` TypeScript types in `src/frontend/src/types/company.ts`.
- **Deliverables**: `src/frontend/src/components/CompanyCardIndex.tsx` (NEW, 168 lines); `src/frontend/src/types/company.ts` (added 4 interfaces); `src/backend/schemas/company.py` (+1 line: `doc_kind` on response schema).
- **Sanity check result**: `npm run build` in `src/frontend` passes (tsc -b && vite build, 613ms, 0 TS errors). Ruff clean on the Python change. TestClient API smoke test against `data/mle_prep.db`: `GET /api/companies/29/documents` returns 6 docs with kinds `{prep_note, card_index}`, card_index doc id=66 has 10 cards and 29 problems (matches seed). Schema default verified: when `doc_kind` omitted on construction, defaults to `prep_note`. Existing backend tests `test_company_prep_endpoint.py` + `test_sync_docs_create.py` all pass (7/7).
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-441 --status completed`
## 2026-04-15 -- [T-P1-442] Pinterest card index: integrate tab=index into PrepNotesPage
- **What I did**: Added a new `'index'` tab to `PrepNotesPage` that renders `CompanyCardIndex`. The tab is only visible when the company has a `card_index` document (derived via `documents.some(d => d.doc_kind === "card_index")`). When the URL has no explicit `?tab=` and no `?doc=` param AND the company has a card_index, `effectiveTab` computes to `"index"` so fresh visits to `/companies/29/prep` land on the card grid (Pinterest). Switched all tab comparison checks from `activeTab` to `effectiveTab` so the rendered content matches the computed default. Added local `indexLcDrawerId` / `indexDbDrawerId` state to open LC/DB drawers over the index without navigating (mirrors DocumentViewer pattern at line 531-532). Added `"index"` to `PrepTab` and `VALID_TABS` in `prepUrlParams.ts`. Added `doc_kind: string` to the `CompanyDocument` interface in `useForumPosts.ts` so the frontend can filter documents by kind.
- **Deliverables**: `src/frontend/src/pages/PrepNotesPage.tsx` (+48/-13 lines); `src/frontend/src/utils/prepUrlParams.ts` (+7/-2 lines); `src/frontend/src/hooks/useForumPosts.ts` (+1 line: `doc_kind`).
- **Sanity check result**: `npm run build` in `src/frontend` passes (tsc -b && vite build, 604ms, 0 TS errors). Full frontend vitest suite: 39/39 passing (11 prepUrlParams + 18 themeFilter + 10 DrawerLayout). Backend pytest `test_company_prep_endpoint.py` + `test_sync_docs_create.py`: 7/7 passing. API smoke test against `data/mle_prep.db`: `GET /api/companies/29/documents` returns 6 docs including `id=66 kind=card_index` -> Index tab appears. `GET /api/companies/1/documents` returns 6 docs all `kind=prep_note` -> Index tab hidden (verified `hasCardIndex=False`).
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-442 --status completed`
## 2026-04-15 -- [T-P1-443] Problems tab: Custom badge + source-type filter switch
- **What I did**: Added a `sourceType` URL param (`all` | `lc` | `custom`, default `all`) to the Problems.tsx `filterSchema`. When sourceType != "all", force `loadAll=true` (limit=1100) and client-side filter on `p.leetcode_id != null` (LC) or `== null` (Custom). Added a 3-button segmented control "All / LeetCode / Custom" above the Problems table (only on the "all" tab, not Blind75/CompanyFreq). Custom problems now display a purple `<Badge variant="purple">Custom</Badge>` next to the title in `renderProblemRow`. Updated the count label to show "Showing X / Y problems" when the filter is active; otherwise the plain "Y problems" label remains. Added client-side pagination slicing (`displayedProblems.slice(page * pageSize, +pageSize)`) so sourceType="lc" (1039 rows) correctly paginates instead of rendering all at once; pagination base switches to `problems.length` when filter is active. Added `sourceType` to the `filterKey` memo so switching filter resets page to 0.
- **Deliverables**: `src/frontend/src/pages/Problems.tsx` (+55/-8 lines, 1 file).
- **Sanity check result**: `npm run build` in `src/frontend` passes (tsc -b && vite build, 598ms, 0 TS errors). Full frontend vitest suite: 39/39 passing (11 prepUrlParams + 18 themeFilter + 10 DrawerLayout). DB sanity via sqlite3: `data/mle_prep.db` has 44 custom problems (leetcode_id IS NULL) and 1039 LC problems (total 1083). Latest 5 custom problems confirmed Snowflake x2, Google x2, Google/Uber x1 tagged correctly. Manual browser verification not performed (autonomous mode); logic traced through the component state flow including: default = paginated 50 rows with Custom badges; sourceType=custom = loadAll + client filter to 44 rows + "Showing 44 / 1083"; sourceType=lc = loadAll + client filter to 1039 + client paginates 200 per page.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-443 --status completed`
## 2026-04-15 -- [T-P1-444] Problems tab: Custom-mode company-grouped card view
- **What I did**: When `sourceType === "custom"` on the All-Problems tab, rendered a responsive grid of company cards instead of the flat table (`grid-template-columns: repeat(auto-fit, minmax(380px, 1fr))`, same pattern as CompanyCardIndex). Added a `customByCompany` memo that groups problems by `company_tags[]` — problems with multiple tags appear under each company card (dedup via `Set` within a problem), problems with empty tags fall into a pinned-last "未归类 / Unassigned" group. Largest groups sort first. The sidebar `company` filter narrows the grid to just cards matching (case-insensitive substring) the entered company. Each card header shows the company name + a purple "N 题" badge; each list item is a `<Link to={/problems/:id}>` with difficulty + done badges. Paging is skipped for the grouped view (only ~44 custom rows exist). Introduced `isCustomGroupedView = isAllTab && sourceType === "custom"` and used it to (a) guard the card render, (b) skip the flat table, (c) hide `<Pagination>`, (d) update the count header to "Showing X custom problems across Y companies".
- **Deliverables**: `src/frontend/src/pages/Problems.tsx` (+75/-6 lines, 1 file).
- **Sanity check result**: `npm run build` passes (tsc -b && vite build, 586ms, 0 TS errors). Full frontend vitest suite: 39/39 passing (11 prepUrlParams + 18 themeFilter + 10 DrawerLayout). DB distribution verified via sqlite3 against `data/mle_prep.db`: 44 custom problems (leetcode_id IS NULL) distributed as Uber 29, Pinterest 7, Google 6, Snowflake 2, DoorDash 1, LinkedIn 1 (overlap from multi-tag problems explains 46 tag-references > 44 rows). Uncategorized=0. Manual browser verification not performed (autonomous mode); logic traced: Custom tab -> grouped cards ordered Uber/Pinterest/Google/Snowflake/DoorDash/LinkedIn with no Unassigned row; clicking a problem title routes to /problems/:id (same drawer-like detail page as the flat table); sidebar Company="pinterest" narrows to the single Pinterest card with 7 rows; switching back to All/LC restores the flat table + pagination.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-444 --status completed`
## 2026-04-16 -- [adhoc] LC 1654 Minimum Jumps to Reach Home seed + notes
- **What I did**: Received user's LC 1654 Python BFS solution via Discord. Built `scripts/add_lc1654_minimum_jumps.py` (idempotent seed in the `add_lc*` family) that upserts the problem into `data/mle_prep.db` with URL, leetcode.ca-fetched description, completion flag, and Chinese solution notes. Notes cover: state-space BFS framing; the `limit = max(f, x) + a + b` upper-bound proof (two-stage argument: beyond f the forbidden constraint disappears and no optimal path benefits from overshooting by more than `a + b`); why `visited` must key on `(pos, last_dir)` not just `pos`; seven pitfall items (off-by-one on backward bound, consecutive-back-jump check direction, `x == 0` short-circuit, etc.); variants/migration notes; one-liner pitch. Pattern='BFS', family='bfs_state_space', tags=['BFS','Graph','State Space','Number Line'].
- **Deliverables**: `scripts/add_lc1654_minimum_jumps.py` (177 lines, committed as 99e37d0).
- **Sanity check result**: Ran the seed — `[ADDED] LC 1654 id=1090`. Verified row via sqlite3: id=1090, url set, difficulty=medium, pattern=BFS, family=bfs_state_space, is_completed=1, description 861 chars, notes 3261 chars. Script idempotent (UPSERT-shaped: existing row path updates notes + marks completed).
- **Status**: [DONE]
- **Request**: No task_db entry — ad-hoc user-initiated LC add, no pre-existing T-ID.
## 2026-04-16 -- [planning] ML topic gap-analysis -> 14-task backlog for Google R1 + Pinterest prep
- **What I did**: Task-planning-mode session. Read-only audit of mle_prep.db coverage (47 depth-2 framework_nodes + 13 Google docs + 6 Pinterest docs) against user-provided 9-category ML rubric (39 subtopics). Built coverage matrix, identified genuinely-missing content vs already-deep-covered content. Planted 14 tasks in task_db (T-P0-227 through T-P2-240) following pyramid-bottom-first rule: P0x7 (ML/DL fundamentals shared by Google+Pinterest), P1x4 (Pinterest-lean CV/NLP/RecSys gaps), P2x3 (generative + SD, restrained pitch-only). Every AC contains explicit char targets and LINK-not-duplicate rules referencing existing Google docs (55/56/60/61/62/63/64/65) and Pinterest docs (58/66) to prevent rework. Added T-P0-233 -> T-P0-231 dependency. Regenerated TASKS.md projection.
- **Deliverables**: 14 task rows in `.claude/tasks.db`; TASKS.md regenerated; no framework_node content written (planning only). Also earlier this session: `scripts/add_lc1654_minimum_jumps.py` + pushed 14 clean commits to origin/main (c3ef731..84ab84f).
- **Sanity check result**: `task_db.py list --status active` returns all 14 tasks with correct priorities/complexities. `task_db.py depend T-P0-233 --on T-P0-231` returned `{"ok": true, "dependency": "T-P0-233 depends on T-P0-231"}`. `task_db.py project` regenerated TASKS.md cleanly. No DB writes beyond tasks.db.
- **Status**: [DONE] for planning phase. Execution blocked on user's explicit go/no-go for autonomous_run.sh launch (user interrupted prior launch attempt). autonomous_run.sh is NOT running.
- **Request**: No task_db.py status changes -- all 14 new tasks remain `active` waiting for execution.
## 2026-04-16 -- [planning] T-P0-241 sketch-family unification meta-task + autonomous runner launch
- **What I did**: User flagged compact-DS content redundancy (CMS/HLL/SS treated as independent primitives across 4 artifacts, with 'HLL' term drift between network-measurement vs DB communities). Audit via mle_prep.db confirmed overlap: framework_node 196 (pillar1.streaming_topk, 7924b canonical), 197 (scaling_resource_model, 8845b re-defines CMS/HLL), 103 (realtime_features, 5216b tangential Key Terms dup), company_document 58 (Pinterest Sketch 1-Pager, 3817b re-does CMS/SS/HLL math). Added T-P0-241 [M] with user's 3-axis framework hard-coded as canonical vocabulary: (1) hash source, (2) counter/register structure, (3) aggregation operator. AC mandates node 196 rewrite as canonical (10000-14000b target, adds CMM median, Bernoulli freq sketch, bitmap register generalization, unified test-once view, production layered-system design), nodes 197/103/doc 58 reduced to 2-line definitions + pointer to 196, HLL family-vs-instance grounding at first mention everywhere. Explicit non-goals prevent scope creep (no Bloom/t-digest/quantile expansion, no distributed-merge deep dive). Verified zero overlap with in-flight tasks T-P0-227 through T-P2-240 (none touch nodes 103/196/197 or doc 58). Launched scripts/autonomous_run.sh 10 MLInterviewPrep in background (pid backgrounded, log logs/autonomous_20260416_001255.log); Session 1/10 active.
- **Deliverables**: Task T-P0-241 in .claude/tasks.db (15 active tasks now); TASKS.md regenerated; background autonomous runner spawned with 10-session budget.
- **Sanity check result**: task_db.py add returned {ok: true, id: T-P0-241}; project regeneration OK. Autonomous runner log confirms 'Session 1/10' reached and a new claude.exe subprocess spawned (ps count went 13 -> 14 after launch). No conflict analysis: greped the 14 existing task descriptions against {node 103, 196, 197, doc 58} -- zero hits.
- **Status**: [DONE] for planning + launch phase. Execution in progress via autonomous_run.sh. 15 active tasks queued: T-P0-227..233 + T-P0-241 (P0, 8 total, runs first by priority+id), T-P1-234..237 (4), T-P2-238..240 (3).
- **Request**: No manual task_db.py status changes -- orchestrator handles transitions per-session.
## 2026-04-16 -- [T-P1-421] Google R1 A/B test rigor drill doc
- **What I did**: Built a Google-R1-oral-prep drill doc covering 5 ACs: (1) sample size n = 2*sigma^2*(z_{alpha/2}+z_beta)^2/delta^2 with derivation showing why the two z quantiles ADD, (2) SRM as chi-square randomization health check with "NOT a business result" framing and common pipeline root causes, (3) CUPED derivation with variance reduction factor (1-rho^2) and pre-period freeze rule, (4) novelty vs primacy with 1-week washout absorbing day-of-week periodicity, (5) Etsy GMB (Google Merchant Bidding) compound trap war story: unit-of-analysis mismatch + auction-density SRM + auction-ecosystem novelty, with cluster-robust SE formula. Mirrors the format of seed_google_calibration_drill.py. Added cross-link from `docs/google_2026-04-17_prep.md` line 52 to the new drill doc while preserving the pointer to the existing pillar7.probability_statistics.ab_test_sample_size foundation note (no duplication).
- **Deliverables**: `scripts/seed_google_ab_test_rigor_drill.py` (new, 300+ lines, idempotent), `docs/google_ab_test_rigor_drill.md` (new, 13,752 chars, 7 FormulaBlocks, 2 comparison tables, 1 checklist), `data/mle_prep.db` company_documents id=67 (company_id=3 Google, doc_kind=prep_note), `docs/google_2026-04-17_prep.md` (1-line cross-link update).
- **Sanity check result**: Seed ran cleanly: `[DONE] Wrote docs/google_ab_test_rigor_drill.md (13752 chars)`, `[DONE] DB document id=67`. StudyNoteBuilder.validate returned no warnings. Re-ran seed; got `[SKIP] Document already exists (id=67)` confirming idempotency. Verified 7 `$$...$$` math blocks via regex extract; all close cleanly. DB row verified via sqlite3: id=67, title matches, content length 13752, source_type=manual, doc_kind=prep_note. Foundation pillar7 node (length=7615) untouched.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-421 --status completed` (applied).
## 2026-04-16 -- [planning] Knowledge-graph audit + 3-task roadmap proposal (pending user approval)
- **What I did**: User raised two meta-concerns: (1) 11 Google R1 drill docs are in English but convention is Chinese prose + English technical terms; (2) Cross-company prep assets (LinkedIn/Uber/Adobe/DoorDash/Google/Pinterest) duplicate core ML concepts rather than linking to a canonical knowledge graph. Read-only scope audit of data/mle_prep.db: 57 company_documents totaling 1.75M bytes across 7 companies -- LinkedIn 465k/6docs, Adobe 401k/10docs, Uber 374k/11docs, DoorDash 314k/9docs, Google 152k/14docs (11 EN + 3 ZH), Pinterest 26k/6docs. Found concrete duplication evidence: Uber doc 28 and Adobe doc 29 share identical title "ML Fundamentals From-Scratch 完整指南" with identical length 151774 bytes -- near-certain byte-for-byte copy across two companies. Identified 47 framework_nodes as natural canonical homes (mostly skeletal descriptions ready to absorb migrated content). Proposed 3-task roadmap via Discord: T-P0-242 [S] knowledge-graph convention (doc_kind taxonomy + link format, no data migration), T-P0-243 [M] cross-company duplication map audit (read-only, 25 concepts x 57 docs), T-P0-244 [M] Google R1 11-doc Chinese translation (deadline-driven for 2026-04-17 R1, archives originals first). Phase 2 per-concept consolidation tasks DELIBERATELY DEFERRED until T-P0-243 audit delivers concrete duplication data -- user's 慎重思考 directive rules out premature task spawning.
- **Deliverables**: Proposal delivered on Discord (message 1494237112960221185). No task_db writes yet (awaiting user decision).
- **Sanity check result**: Audit queries verified against data/mle_prep.db -- doc counts/sizes reproducible. Translation scope verified by scanning first 200 chars per Google doc for CJK frequency (11 EN confirmed: 52/55/56/57/60/61/62/63/64/65/67; 3 ZH confirmed: 38/51/53). No task_db writes = no projection drift.
- **Status**: [PARTIAL] Planning proposal delivered; task_db additions BLOCKED pending user approval ("要我把这三条加入 task_db 吗? 还是先调整 scope?"). Intentional planning-mode boundary -- per CLAUDE.md task-planning rules, user alignment precedes task_db mutation for meta-scope proposals.
- **Request**: Awaiting user go/no-go on the 3 tasks + whether to prepend T-P0-244 (deadline) ahead of the 8 already-active P0s in autonomous queue.
## 2026-04-16 -- [T-P1-422] Google R1 feature drift monitoring drill doc
- **What I did**: Built a Google-R1-oral-prep drill doc covering 5 ACs: (1) PSI = sum (a - e) * ln(a / e) with 0.1 warn / 0.25 critical thresholds, bin-fix-on-reference discipline, and a worked 4-bin numerical anchor landing in the warn band; (2) KL vs JS contrast -- KL asymmetric / unbounded / blows up on zero support, JS symmetric / bounded by ln 2 / sqrt(JS) is a proper metric; (3) KS continuous test = sup |F1 - F2|, critical value ~1.36 * sqrt((n1+n2)/(n1*n2)) at alpha=0.05, tail-weakness caveat; (4) three shift types -- covariate (P(x) moves -> re-weight w(x)=P_target/P_source), concept (P(y|x) moves -> relabel+retrain), label (P(y) moves -> recalibrate via Saerens/BBSE) with diagnostic flow and the "P(x) drift necessary but not sufficient for covariate shift" trap; (5) per-feature + per-segment + per-time-window alerting with Benjamini-Hochberg FDR formula and alert-fatigue countermeasures. Mirrors seed_google_ab_test_rigor_drill.py format. Added cross-link from `docs/google_2026-04-17_prep.md` line 53 to the new drill doc.
- **Deliverables**: `scripts/seed_google_feature_drift_drill.py` (new, ~330 lines, idempotent), `docs/google_feature_drift_drill.md` (new, 15,163 chars, 8 FormulaBlocks, 2 comparison tables, 1 checklist), `data/mle_prep.db` company_documents id=68 (company_id=3 Google, doc_kind=prep_note), `docs/google_2026-04-17_prep.md` (1-line cross-link update on line 53).
- **Sanity check result**: Seed ran cleanly: `[DONE] Wrote docs/google_feature_drift_drill.md (15163 chars)`, `[DONE] DB document id=68`. StudyNoteBuilder.validate returned no warnings. Re-ran seed; got `[SKIP] Document already exists (id=68)` confirming idempotency. Verified 16 `$$` markers (= 8 FormulaBlocks, each paired) via python count. DB row verified: id=68, company_id=3, title matches, content length 15163, doc_kind=prep_note.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-422 --status completed` (applied).
## 2026-04-16 -- [T-P0-242] Knowledge Graph design doc v1 (pre-migration research)
- **What I did**: User requested a concrete, reviewable design before committing to any cross-company content migration. Reserved T-P0-242 via status=in_progress to keep autonomous_run.sh from picking it up. Verified the smoking-gun duplicate (SHA256 hash comparison): company_document id=28 (Uber) and id=29 (Adobe) share identical hash `3f2db8f9287fc95d91e462577166ca5934a2b2096d5c206fc657ed4b7ef31d51` at 151774 bytes -- confirmed byte-for-byte cross-company copy of 'ML Fundamentals From-Scratch 完整指南'. Wrote 367-line design doc covering: (§1) problem statement, (§2) current-state audit w/ evidence, (§3) target data model (framework_node=canonical + company_document=composition + optional concept_links table), (§4) link convention syntax `> **正典** [Title (path)](/framework/N)` + back-link rendering spec, (§5) visualization recommendation (React Flow + dagre, /kg route), (§6) 4-phase migration strategy (design review -> convention+schema -> 3 high-value concept consolidations -> systematic -> frontend viz), (§7) 13-entry risk register + explicit non-goals, (§8) 6 success metrics w/ baseline-to-target deltas, (§9) 10 numbered open questions w/ recommended defaults for user decision, (§10) 13-entry preliminary duplication hotspot catalog estimating 200-300kb collapsible (~15% of corpus). Design is user-review-only: zero schema changes, zero content migration, zero new code outside the doc itself.
- **Deliverables**: `docs/knowledge_graph_design_20260416.md` (367 lines, commit 5bbd2aa). T-P0-242 marked completed in task_db.
- **Sanity check result**: `wc -l` = 367; git commit clean; pre-commit hook passed (no secrets). Content sanity: confirmed the concrete duplicate evidence via SQL hash query before asserting it in the doc. Open-question defaults reflect tradeoffs explained in the body.
- **Status**: [DONE] for design phase. Phase 1 (schema + convention + empty /kg scaffold) BLOCKED pending user review of §9 open questions, specifically Q5 (Phase-2 third consolidation target -- default Regularization), Q6 (Google R1 11-doc translation timing -- default BEFORE Phase 1 due to 2026-04-17 R1 deadline), Q10 (canonical scope -- default pillar2/3/4/6 ML-core only).
- **Request**: `task_db.py update T-P0-242 --status completed` already applied.
## 2026-04-16 00:36 -- [T-P1-423] Google R1 train-serve skew/leakage/time-split drill doc
- **What I did**: Built a Google-R1-oral-prep drill doc covering 5 ACs: (1) target encoding K-fold OOF formula + smoothing `(n_c * local + m * global) / (n_c + m)` + two compound gotchas (GroupKFold by session, per-fold global-mean prior); (2) why ranking MUST use time-based / walk-forward split -- popularity leakage, session bleed, and concept-drift absorption, with the explicit note that user-level random split fixes cold-start but NOT temporal skew; (3) feature-store parity with three canonical skew flavors -- timestamp/AS-OF, null semantics (NULL vs sentinel), fill/default (mean vs zero) -- plus a fourth computation/float-precision flavor as a bonus, and the nuclear fix "log serving-time values, join by request_id"; (4) label leakage via future-only aggregates -- own-row inclusion, future-window bleed, wrong-granularity join, label_time_ns discipline + shuffle-timestamp regression test; (5) a Candidate-Story production gotcha from Etsy creator-level target encoding (+1.2 offline NDCG -> flat/negative online GMB, root-caused to session bleed across OOF folds + full-data global-mean prior). Mirrors seed_google_feature_drift_drill.py format. Added cross-link from `docs/google_2026-04-17_prep.md` Round-1 data/features bullet (line 41).
- **Deliverables**: `scripts/seed_google_train_serve_skew_drill.py` (new, idempotent, mirrors feature-drift-drill format), `docs/google_train_serve_skew_drill.md` (new, 22,121 chars, 5 FormulaBlocks, 2 comparison tables, 3 interview Q&A, 1 checklist), `data/mle_prep.db` company_documents id=69 (company_id=3 Google), `docs/google_2026-04-17_prep.md` (1-line cross-link added to bullet 2 of Round 1).
- **Sanity check result**: Seed ran cleanly: `[DONE] Wrote docs/google_train_serve_skew_drill.md (22121 chars)`, `[DONE] Inserted document id=69`. StudyNoteBuilder.validate returned no warnings. Re-ran seed; got `[SKIP] Document already exists (id=69)` confirming idempotency. Verified 10 `$$` markers (= 5 FormulaBlocks, each paired) via python count. DB row verified: id=69, company_id=3, title matches, content length 22121.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P1-423 --status completed`
## 2026-04-16 01:00 -- [T-P2-438] Remove duplicate httpx from dev optional-dependencies
- **What I did**: Removed the duplicate `httpx==0.27.2` entry from `[project.optional-dependencies].dev` in pyproject.toml. httpx remains in `[project].dependencies` (main), so `pip install -e .[dev]` still gets it -- just once, not twice.
- **Deliverables**: `pyproject.toml` (1-line edit: dev list went from 5 items to 4 items).
- **Sanity check result**: Parsed pyproject.toml via tomllib: `main has httpx: True`, `dev has httpx: False`, dev list = [pytest, pytest-asyncio, ruff, pyyaml]. No other consumers of the dev extra reference httpx directly.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P2-438 --status completed`
## 2026-04-16 01:30 -- [T-P2-437] Propagate 4 new lessons to helixos LESSONS.md
- **What I did**: Appended 4 MLInterviewPrep lessons (2026-04-10 through 2026-04-15) to `helixos/LESSONS.md` using the existing `[PROPAGATED]` naming pattern. Each entry includes Source attribution + an explicit "Helixos has the same shape" connection: (1) validation on production-isomorphic surfaces -- helixos has matching SQLite/WAL/tsc-b build/React UI surfaces; (2) react-markdown v10 urlTransform strips custom schemes -- helixos uses react-markdown in MarkdownRenderer/ConversationView; (3) orchestrator all_done sticky flag -- helixos uses the same autonomous_run.sh/ps1; (4) auto-bolding inside LaTeX/code leaks ** -- applies to any helixos prose post-processing touching KaTeX content. Rewrote each lesson in the helixos-appropriate voice where applicable, preserved all tag taxonomies, and added `#propagated`.
- **Deliverables**: `helixos/LESSONS.md` (+76 lines appended, 297 -> 329 lines). No other files touched.
- **Sanity check result**: `grep -n "\[PROPAGATED\]" helixos/LESSONS.md` shows 9 propagated entries with dates in chronological order (2026-03-20, 04-07, 04-08 x3, 04-10, 04-13 x2, 04-15). All 4 new sources match MLInterviewPrep/LESSONS.md dates.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P2-437 --status completed`
## 2026-04-16 02:00 -- [T-P2-439] Move scraper deps to requirements-scraper.txt
- **What I did**: Created `requirements-scraper.txt` holding the 2 optional scraper pins (beautifulsoup4==4.12.2, playwright==1.58.0) and removed them from `requirements.txt`, so default `pip install -r requirements.txt` no longer pulls scraper-only chains (playwright + its bundled browsers) in non-scraper dev envs. Updated `.github/workflows/ci.yml` jobs (lint, test, migration-test) to install both files (scraper tests exist: test_extractors.py, test_crawler_cdp_cookie.py, etc.), and added optional-install notes to README.md and scripts/QUICKSTART.md (bash + PowerShell blocks).
- **Deliverables**: new `requirements-scraper.txt` (2 deps + header comment); edited `requirements.txt` (removed bs4+playwright, added pointer comment); edited `.github/workflows/ci.yml` (3 CI jobs now install both files); edited `README.md` + `scripts/QUICKSTART.md` (optional-install hint).
- **Sanity check result**: Wrote a tomllib-based diff check: `requirements.txt` set equals pyproject project deps + dev extra; `requirements-scraper.txt` set equals pyproject scraper extra. Output: `SYNC OK`. Confirms the two requirements files now exactly mirror pyproject.toml classification and debt is cleared.
- **Status**: [DONE]
- **Request**: `task_db.py update T-P2-439 --status completed`
## 2026-04-16 -- [T-P0-243] Phase 0.5: framework_node content template (UX-first reframe)
- **What I did**: User reviewed KG design v1 + a third-party reviewer critique, surfaced that BOTH missed the real requirement: per-node one-stop coverage + progressive-disclosure drawer, NOT cross-doc graph viz. Reserved T-P0-243 status=in_progress to prevent autonomous_run.sh pickup. Wrote 328-line content template covering: (§1) 60-second 80% reader contract, (§2) granularity rule formalizing user's 'domain verticals split / foundational horizontals consolidate' principle with 10-row worked decision table + verbatim capture of user's LLM/TreeModel/L2R/Stats guidance, (§3) 5-section always-visible spine (一句话定义+Intuition / 核心公式 / Tradeoffs / 面试常见问法 / Prerequisites) with ≤3000b budget, (§4) 6-tab optional drawer menu (derivation/code/variants/interview_deep/see_also/history) with per-tab byte budgets, (§5) drawer mechanism decision: markdown heading convention `## Drawer: {key}` inside framework_node.description -- zero schema change, zero HTML comments, explicitly rejected reviewer's `<!-- KG:67:reference -->` proposal as YAGNI, (§6) writing rules (Chinese prose default, English math/code/algo names, curated see_also back-links), (§7) fully-worked Sketch sample populating the template for framework_node 196, (§8) phase realignment (original Phase 1 concept_links/graph scaffold punted to Phase 4+; Phase 2 retargeted to 1 concept first via T-P0-241), (§9) 8 numbered open questions w/ recommended defaults, (§10) explicit non-decisions. Declares sections 3-5 of the prior KG design v1 doc superseded.
- **Deliverables**: `docs/framework_node_content_template_20260416.md` (328 lines, commit 75b8f34). T-P0-243 marked completed in task_db.
- **Sanity check result**: `wc -l` = 328; pre-commit hook clean; markdown structure verified (TOC numbering contiguous, all 10 sections present, Sketch sample renders valid LaTeX + code blocks). No DB writes; no schema changes; no content migration touched by this doc.
- **Status**: [DONE] for Phase 0.5 template. Phase 1 simplified scope (CLI helper only, plus shadcn drawer component decision) BLOCKED pending user review of §9 Q1-Q8. Most time-urgent responses: Q3 (drawer mechanism -- markdown convention vs new table), Q8 (pillar7 consolidation granularity).
- **Request**: `task_db.py update T-P0-243 --status completed` already applied.
## 2026-04-16 -- [T-P0-244] Phase 0.5a: template v1.0.1 tightening (pre-Sketch)
- **What I did**: User forwarded a second independent reviewer's 4-point critique of Phase 0.5 template v1 + 8 open-Q answers. Planted T-P0-244 (v1.0.1 pre-Sketch) + T-P1-245 (v1.1 post-Sketch deferred). Wired dependency chain: T-P0-244 → T-P0-241 → T-P1-245 so the Sketch task cannot start until template is tightened, and v1.1 cannot start until Sketch delivers real-world signal. Reserved T-P0-244 status=in_progress. Applied 6 in-place edits to `docs/framework_node_content_template_20260416.md`: (a) §3.2 gains 'statement-of-result only' boundary rule vs drawer.derivation, (b) §3.4 mandates sourcing-tag on every interview framing with three-choice schema `[实际来自:doc-{id} §{sec}] / [改编自:{src}] / [预期问法:{reasoning}]`, unsourced = PR block, (c) new §4.1.1 establishes 20% budget overflow as a split-signal linking drawer discipline to §2 granularity, (d) §5.3 adds parse-marker rule preventing drawer-heading double-render + main-body rendering below first drawer marker, (e) new §6.5 codifies PR-description next-candidates convention (siblings + inline-linked nodes) for natural migration-queue, (f) new §2.5 escape-hatch documents when pillar7 consolidation legitimately splits. Deferred to v1.1 per reviewer: Optimization (SGD/Adam/二阶) worked granularity example and canonical drawer-tab render order -- both require Sketch-experience signal before committing. Runner anomaly discovered: `scripts/autonomous_run.sh` 10 sessions completed but NONE of T-P0-227..241 progressed; sessions 7-10 logged 'all_done=true / no unblocked tasks' despite 15 active P0/P1/P2 tasks waiting. Suspected stale `session_state.json` ({last_task:T-P0-220, remaining:[198,193]}) from pre-session seed never refreshed to reflect new batch.
- **Deliverables**: `docs/framework_node_content_template_20260416.md` updated (369 lines, +43/-2; commit bb53142). Tasks T-P0-244 (completed), T-P1-245 (active, blocked on T-P0-241), T-P0-241 now explicitly blocked on T-P0-244 (now unblocked since 244 completed).
- **Sanity check result**: `wc -l` = 369 (from 328 prior = +41 net); git diff confirms 6 Edit operations applied in intended places; pre-commit hook clean; dependency chain verified via `task_db.py list`: T-P0-241 now shows T-P0-244 as satisfied dependency. Revision log in template updated with v1.0.1 entry + planned v1.1 entry.
- **Status**: [DONE] for v1.0.1 tightening. T-P0-241 Sketch execution UNBLOCKED. v1.1 task T-P1-245 remains BLOCKED on T-P0-241 (by design). Autonomous runner bug flagged to user with three options (diagnose / reset session_state / run T-P0-241 manually) -- user decision pending.
- **Request**: `task_db.py update T-P0-244 --status completed` applied.
## 2026-04-16 -- [runner-diag] autonomous_run.sh no-op root cause: two task_db files
- **What I did**: Diagnosed why the 10-session autonomous_run.sh burned through without progressing any planted T-P0-227..241 or T-P1/P2 batch. Inspected `scripts/autonomous_run.sh`, `.claude/hooks/task_db.py:_find_project_root()`, and both databases. Confirmed: `_find_project_root` picks the nearest `CLAUDE.md` starting from cwd; Gen_AI_Proj and MLInterviewPrep each have their own, so root `.claude/tasks.db` and `MLInterviewPrep/.claude/tasks.db` are distinct stores. My earlier `task_db.py add` calls from root cwd deposited all 16 active tasks (plus T-P0-242/243/244 design tasks, T-P1-245) into ROOT's tasks.db. The runner launched with `project_dir=MLInterviewPrep` cd'd into that directory each session, ran `has-unblocked` against MLInterviewPrep's local store (0 active), correctly got 'no', set all_done=true, and subsequent sessions short-circuited. Ran `python .claude/hooks/task_db.py has-unblocked` from both cwd to reproduce: root returns 'yes' exit 0; MLInterviewPrep returns 'no' exit 1. Also discovered T-P2-239 ID collision across the two DBs (different content), so naive migration requires explicit ID remap.
- **Deliverables**: Diagnosis-only -- no code fix yet. Runner itself is not buggy; my planting was mis-scoped.
- **Sanity check result**: `has-unblocked` returns contradictory values across cwd, confirming dual-DB theory. `sqlite3 .claude/tasks.db 'SELECT COUNT(*) FROM tasks WHERE status=active'` returns 16 (root); same query on MLInterviewPrep.tasks.db returns 0. T-P2-239 collision confirmed via set intersection of IDs.
- **Status**: [BLOCKED] pending user's choice among 3 fix options (A migrate tasks to MLInterviewPrep.tasks.db / B change orchestrator DB scope / C keep status quo). User also approved switch from `run_in_background` handoff to foreground-monitored `claude -p` invocations for the next execution round.
- **Request**: No task_db.py status changes. Fix will follow user's A/B/C decision.
## 2026-04-16 -- [runner-fix] Option A migration + pre-session has-unblocked guard
- **What I did**: User approved Option A (migrate + fix runner). Wrote `scripts/migrate_active_tasks_to_mlp_20260416.py` (one-shot, non-idempotent): dumped 16 active tasks from root `.claude/tasks.db` via sqlite to `tmp_migration.json` capturing title/priority/complexity/description + 3 dep-relationships, then from MLInterviewPrep cwd re-`add`'d each via `task_db.py` (global ID counter assigned new IDs T-P0-445..T-P2-460), re-wired 2 in-set deps via `task_db.py depend` (T-P0-451→T-P0-449 was 233→231; T-P1-457→T-P0-452 was 245→241), skipped 1 dep where upstream wasn't in migration set (T-P0-241→T-P0-244: 244 already completed in root = satisfied), `delete`'d all 16 originals from root, ran `project` on both DBs to regenerate TASKS.md. Wrote audit trail `logs/task_migration_20260416_map.json` with old→new map. Cleaned 3 stray zero-byte files (`code`, `derivation`, `history`) created by earlier bash interpreting arrow-separated strings as redirects. Added authoritative pre-session check to `scripts/autonomous_run.sh` at top of main loop: `python .claude/hooks/task_db.py has-unblocked` must return exit 0 (yes) before spawning a claude CLI session. This bypasses session_state.json drift -- session_state is advisory, tasks.db is truth. Bash-syntax-checked the edited script (`bash -n`).
- **Deliverables**: (a) `scripts/migrate_active_tasks_to_mlp_20260416.py` one-shot migration tool. (b) `scripts/autonomous_run.sh` +9 lines pre-session guard. (c) `logs/task_migration_20260416_map.json` audit trail. (d) 16 new tasks in MLInterviewPrep tasks.db (T-P0-445..T-P2-460). (e) 16 deletions from root tasks.db. Root commit `98d6cc4` (master branch, Gen_AI_Proj); MLP commit `1022be8` (main branch, MLInterviewPrep, TASKS.md projection).
- **Sanity check result**: has-unblocked from root cwd returns `no` exit 1 (correct -- root has 0 active); has-unblocked from MLInterviewPrep cwd returns `yes` exit 0 (correct -- 16 active). Lowest-ID P0 unblocked = T-P0-445 (Cost-sensitive model selection) with depends_on=None -- the right first pick. `bash -n scripts/autonomous_run.sh` passed.
- **Status**: [DONE] for migration + runner fix. Pre-commit hook clean on both commits. Awaiting user's go signal for foreground-monitored T-P0-445 session (user previously opted for monitored not background execution).
- **Request**: No task_db status changes -- the 16 migrated tasks remain `active` in MLP, status will flip as sessions execute them.

## 2026-04-16 -- [runner-launch] Monitored single-session kickoff for T-P0-445
- **What I did**: Per user's "有条件的话可以开始逐个 queue 了跑了" approval, launched `bash scripts/autonomous_run.sh 1 MLInterviewPrep` in background (max_sessions=1 to validate runner fix before committing to a longer batch). Output piped to `logs/autonomous_monitored_20260416_*.log`. Runner's pre-session `has-unblocked` check should pass (verified manually minutes before: T-P0-445 is active with no deps). Scheduled a 270s wakeup to check progress without polling.
- **Deliverables**: One backgrounded bash process (autonomous_run.sh); log file at logs/autonomous_monitored_20260416_*. No direct task_db mutations from this context -- subprocess claude session owns T-P0-445 status transitions (active -> in_progress -> completed). Not reserving T-P0-445 in_progress from main context to avoid confusing the subprocess's own pick logic.
- **Sanity check result**: Background launch succeeded (bash detached via &); sleep+ls confirmed log file created. Subprocess session activity will appear as new commits on MLInterviewPrep main branch; detection at next wakeup via `git log origin/main..HEAD`.
- **Status**: [PARTIAL] kicked off; awaiting session completion signal. Will report to user via Discord with diff + commit + summary at wakeup, then request go for next task.
- **Request**: T-P0-445 status updates are subprocess-owned; this main context intentionally does NOT mark in_progress/completed from here.

## 2026-04-16 -- [T-P0-445] Cost-sensitive model selection rubric + Pinterest/Google cases
- **What I did**: Built `scripts/seed_ml_cost_sensitive_selection.py` (one-shot seeder) that (1) generates `docs/ml_cost_sensitive_selection.md` via `StudyNoteBuilder` with a 4-step decision rubric, lever-ladder cheatsheet table, and two case studies on opposite ends of the cost spectrum -- Pinterest unsafe-content (C_FN >> C_FP, 1000:1) and Google Ads policy filter (C_FP > C_FN, 20:1 inverse) -- plus an interview-oral script and 8-item self-check; (2) writes an expanded description (3221 bytes, bilingual EN+CN) into `framework_nodes.id=17` (Model Selection & Validation), covering the same rubric, a worked-numbers table (4 rows) showing that default tau=0.5 costs 35% more EC than the rubric-chosen point, sister-node handoffs to nodes 16/70 and doc 62, and 4 pitfalls. The hub doc references `docs/google_calibration_drill.md` (doc id=62) twice without duplicating any Platt/Isotonic/Temperature math. Hit orphan-dollar-sign fail-fast on first run (USD literals like `$500` collided with StudyNoteBuilder's single-dollar check); resolved by switching prose USD values to "500 USD" notation while keeping proper LaTeX math in FormulaBlocks.
- **Deliverables**: (a) `docs/ml_cost_sensitive_selection.md` -- new hub doc, 9964 chars / 1640 words (target <=2500 words). (b) `scripts/seed_ml_cost_sensitive_selection.py` -- idempotent-ish seeder (rewrites doc and node description on rerun). (c) `data/mle_prep.db` framework_nodes id=17 description: 0 -> 3221 bytes (target >=3000). No new company_documents row -- hub doc is framework-level, not company-scoped (aligning with existing `prep_*.md` convention).
- **Sanity check result**: Seeder prints `[OK] All acceptance checks passed (node=3221 bytes, doc=1640 words)`. StudyNoteBuilder's orphan single-dollar check passes. Spot-checked node 17 description start/middle/end for encoding (bilingual chars render correctly) and hub doc head (HTML comment + prereqs + key terms + Section 0 + EC formula all intact).
- **Status**: [DONE]
- **Request**: `task_db.py update T-P0-445 --status completed`
