# Scraper-only dependencies (pyproject.toml [project.optional-dependencies].scraper).
# Install alongside requirements.txt when running forum-scraper code or its tests:
#   pip install -r requirements.txt -r requirements-scraper.txt
beautifulsoup4==4.12.2
playwright==1.58.0
