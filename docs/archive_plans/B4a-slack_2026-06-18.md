# B4a Slack -- Archive Dry-Run Plan (2026-06-18)

> Per `docs/workflow/company_internalization_protocol.md` -- the dry-run gate. WRITES NOTHING TO DB. User reviews §2 (causal-proof matrix) and §5 (promotion candidates), then explicitly approves before any apply step. Silence is not the gate.

- **Company**: Slack (id=32, status=phone_screen)
- **Audit source**: `docs/audit/company_kg_internalization_audit_2026-05-10.md` (T-P1-798); row: S1 0B / S2 372B / S3 15 181B (1 doc, 26 headings) / S4 0 / S5 0 / S6 0
- **Surfaces in scope**: S2 (admin notes, 372B) + S3 (1 doc, 13 791 chars / 15 181 bytes). S1 / S4 / S5 / S6 are empty.
- **Apply gate**: explicit "ok 执行" from user.

## TL;DR (the headline finding -- read this first)

Slack's S3 is **not** the duplicated-reusable-prose class this protocol primarily targets (the way Uber/Pinterest study-note docs are). It is a **single recruiter/HR-screen prep artifact** (`Slack (Salesforce) ML -- HR Call Prep`, doc id=59), authored for a one-time call dated **2026-04-15** -- a date now ~2 months past. Its content decomposes into four kinds:

1. **Already-internalized facts** -- the call schedule is already a row in `interview_events` (id=31), and the two project pitches are already `behavioral_examples` (id=6 Allocation, id=13 LLM pipeline). Provably no loss on archive.
2. **Generic recruiter-screen mechanics** -- map cleanly to the existing meta-prep node `kg://298` (Pre-Loop: Recruiter Call + Tech Screen) and `kg://251` (STARR delivery).
3. **Genuinely Slack-specific product context** -- Slack AI / Agentforce / Einstein / Data Cloud surface map. NOT de-companiable; this is the one block that must be *retained* (in the §3 skeleton), not dropped.
4. **Ephemeral one-time logistics + personal verbatim scripts + cheat sheet** -- no KG target and not reusable. Surfaced as an **open question** in §2 (drop vs keep-as-historical), never silently dropped.

There is exactly **one** promotion candidate (a recruiter-screen-tactics cluster: comp-anchoring, why-leaving framing, red-flags, questions-to-ask), and it is currently at **2/11 companies (Slack + Uber)** -- **below** the >=3 promotion threshold. So §5 flags it as **WATCH, do not promote yet**.

Net: this is a low-risk, low-yield archive. The recommendation (see §4 preamble) is a **content-swap** (S3 doc body -> thin skeleton), not a hard DELETE, plus two open questions for you.

---

## §1 Inventory snapshot

### S1 -- `companies.prep_notes` (0 bytes)

Empty (`prep_notes IS NULL`). Nothing to migrate.

### S2 -- `companies.notes` (372 bytes, 0 headings, candidate_% = 25.0)

Full content:

```
Slack ML team (Salesforce ownership). HR call 2026-04-15 Wed 14:00 EST = 11:00 PT, 30-45 min.
Prep doc: docs/slack_hr_call_prep.md. Self-intro, why-Slack, main story, comp, 3 questions.
Target comp: $400-550K TC for Senior MLE. Timeline: 4-6 weeks.
Slack ML hot spots: Slack AI (summary, search answers), channel/message ranking, enterprise search, Agentforce integration.
```

Four admin lines: (1) team ownership + **schedule** (already in `interview_events` id=31); (2) a stale `docs/slack_hr_call_prep.md` pointer + agenda; (3) comp target + timeline (ephemeral, personal); (4) Slack ML hot-spot list (duplicates the richer Part 3 of S3). candidate_% = 25 (S2 is admin-shaped, not prose-for-archive).

### S3 -- `company_documents` (1 doc, 13 791 chars / 15 181 bytes, 26 H2/H3 headings, kg_refs=0, drawer_links=0, candidate_% = 79.7)

| id | bytes  | doc_kind  | source_path                          | title                                  |
|---:|-------:|-----------|--------------------------------------|----------------------------------------|
| 59 | 15 181 | prep_note  | scripts/seed_slack_hr_call_prep.py   | Slack (Salesforce) ML -- HR Call Prep  |

Section map (the archive candidates, by heading):

| # | Heading | char-range (approx) | nature |
|---|---|---|---|
| H | `> 2026-04-15 ... Recruiter Screen` (header blockquote) | 0-520 | schedule + mindset |
| P0 | `Part 0: 通话前 30-min checklist` + 确认基本信息 | 520-1500 | generic call mechanics + Slack-specific Qs |
| P1 | `Part 1: Self-introduction` (script + self-check) | 1500-3300 | personal verbatim script + 2 stories |
| Q1 | `Q1: Tell me about yourself` | 3300-3400 | pointer to P1 |
| Q2 | `Q2: Why are you looking / leaving eBay` | 3400-4100 | generic recruiter tactic |
| Q3 | `Q3: Why Slack / Why this role` | 4100-5400 | Slack-specific product alignment |
| Q4 | `Q4: ML project you're proud of` | 5400-7300 | 2 stories (Allocation + LLM-Judge) |
| Q5 | `Q5: Comp expectation` | 7300-8400 | generic comp-negotiation tactic |
| Q6 | `Q6: Timeline` | 8400-8900 | ephemeral + generic phrasing |
| Q7 | `Q7: Any questions for me` (3 Qs + bonus) | 8900-10600 | generic questions-to-ask template + Slack flavor |
| P3 | `Part 3: Slack / Salesforce ML context` | 10600-12400 | **Slack-specific product knowledge** |
| P4 | `Part 4: Red flags` | 12400-12900 | generic recruiter tactic |
| P5 | `Part 5: Call 后 action items` | 12900-13400 | ephemeral one-time + workflow |
| P6 | `Part 6: 速查表` (cheat sheet) | 13400-13700 | derived summary, ephemeral |
| AP | `Appendix: 关键数字` | 13700-13791 | resume metrics (already in stories) |

---

## §2 Migration matrix (4-tuple causal-proof)

One row per archive candidate. "现迁移到" is a concrete URI (`kg://N`=framework_nodes.id, `db://N`=other DB row -- here `behavioral_examples`/`interview_events`, `cd://N`=company_documents.id, `sd://<slug>`=system_designs) or `[DROP]`/`[KEEP]`/`[PROMOTION->§5]`/`[OPEN-Q]`. "可验证查询" is runnable (SQLite `mode=ro`).

| # | 原 prose 摘要 (<=80 chars) | 原覆盖 (surface + range) | 现迁移到 | 可验证查询 |
|---|---|---|---|---|
| H1 | Schedule: 2026-04-15 Wed 11:00 PT recruiter screen 30-45 min | S3 header, c0-160 | `db://31` (interview_events) | `SELECT scheduled_at,title FROM interview_events WHERE id=31` -> `2026-04-15 11:00:00 / Slack HR Call` |
| H2 | "recruiter screen ~25% 淘汰率, 当成正式第一轮" mindset | S3 header, c160-520 | `kg://298` (pre-loop recruiter, "~25% 候选人在这阶段被刷掉") | `SELECT description FROM framework_nodes WHERE id=298` (grep "25%") |
| P0a | 30-min pre-call checklist (耳机/网络/静音/心态) | S3 P0, c520-1100 | `kg://298` (generic recruiter-call mechanics) | `SELECT title FROM framework_nodes WHERE id=298` -> Pre-Loop: Recruiter Call + Tech Screen |
| P0b | "确认 role title / team name / loop 轮数" opening Qs | S3 P0, c1100-1500 | `kg://298` (generic) + Slack flavor [KEEP in §3] | `SELECT id FROM framework_nodes WHERE id=298` |
| P1a | Self-intro **structure** (current role / projects / why) | S3 P1, c1500-1700 | `kg://251` (STARR delivery) | `SELECT title FROM framework_nodes WHERE id=251` -> STAR / STAR-T / STARR Delivery Framework |
| P1b | Self-intro **verbatim script** (eBay MLE 3yrs, 94%/90%) | S3 P1, c1700-3000 | `[OPEN-Q]` personal verbatim, ephemeral | metrics provable via AP row; script itself has no reusable home |
| P1c | The two quantified stories referenced in intro | S3 P1, c1900-2600 | `db://6`, `db://13` | see Q4a / Q4b |
| Q2 | "Why leaving eBay" positive-frame talking points | S3 Q2, c3400-4100 | `[PROMOTION->§5]` recruiter-tactics (2/11, watch) | n/a (no target yet -- §5) |
| Q3a | "Why Slack" framework (product/exp-match/specific) | S3 Q3, c4100-4400 | `kg://298` (generic why-company prep) | `SELECT id FROM framework_nodes WHERE id=298` |
| Q3b | Slack-specific product alignment (Slack AI/Agentforce) | S3 Q3, c4400-5400 | `[KEEP in §3 skeleton]` Slack flavor, not de-companiable | grep "Slack AI" in new skeleton (post-apply) |
| Q4a | Primary story: Ranking-as-Allocation (+1% GMB, site-scale) | S3 Q4, c5400-6700 | `db://6` (+ seed `db://2`, related `db://28`/`db://33`) | `SELECT title FROM behavioral_examples WHERE id=6` -> Allocation Framework as Reusable Platform Primitive. **Figure note**: doc's "+1% GMB first experiment" = the diversity seed `db://2` (`...WHERE id=2` carries +1%); id=6 stores the *reuse* figure (+0.6%/reuse, 200M+ total). |
| Q4b | Backup story: LLM-as-Judge eval (94% cost, 90% latency) | S3 Q4, c6700-7300 | `db://13` (+ `db://10` rigor) | `SELECT result FROM behavioral_examples WHERE id=13` -> "~18K labels/day at ~$500 vs $0.30-0.80/label". **Figure note**: the doc's "94% cost reduction" is the *derived* framing of these raw numbers; the literal "94%" string is NOT stored (substance preserved, derived % is a presentation artifact). |
| Q5 | Comp-anchoring tactic ("ask band first, $400-550K") | S3 Q5, c7300-8400 | `[PROMOTION->§5]` recruiter-tactics (2/11, watch) | n/a (no comp-negotiation node exists -- §5) |
| Q6 | Timeline phrasing ("4-6 weeks, in loops") | S3 Q6, c8400-8900 | `[DROP]` ephemeral personal + generic phrasing | n/a (no information value to preserve) |
| Q7 | Questions-to-ask template (team / roadmap / loop) | S3 Q7, c8900-10600 | `kg://298` + `kg://300` (BQ round) + Slack flavor [KEEP §3] | `SELECT title FROM framework_nodes WHERE id IN (298,300)` |
| P3 | Slack/Salesforce ML surface map (AI, ranking, search, Agentforce, Einstein, Data Cloud) | S3 P3, c10600-12400 | `[KEEP in §3 skeleton]` -- the one genuinely Slack-specific knowledge block | grep "Agentforce" in new skeleton (post-apply) |
| P4 | Red flags / things-not-to-say in recruiter call | S3 P4, c12400-12900 | `[PROMOTION->§5]` recruiter-tactics (2/11, watch) | n/a (no target yet -- §5) |
| P5 | Call-后 action items (thank-you email, record into DB) | S3 P5, c12900-13400 | `[DROP]` one-time workflow, already executed | n/a (call occurred 2026-04-15) |
| P6 | 速查表 cheat sheet (1-line per scenario) | S3 P6, c13400-13700 | `[DROP]` derived summary of rows above | n/a (no unique claim) |
| AP | 关键数字 (+1% GMB, 94%, 90%, $400-550K, 4-6wk) | S3 AP, c13700-13791 | `db://2`/`db://6` (GMB), `db://13` (eval raw), Q5 ($400-550K -> §5) | `SELECT result FROM behavioral_examples WHERE id IN (2,6,13)`. **Figure note**: "94% cost" AND "90% latency" are BOTH derived headline framings -- neither "94%" nor "90%" nor "latency" is stored verbatim in any behavioral_example (confirmed by read-only grep); only id=13's raw cost numbers ($500/18K) are. "$400-550K"/"4-6wk" are ephemeral personal facts (-> §5 / DROP). |
| S2 | companies.notes 4 lines (schedule/comp/hot-spots) | S2, full | `db://31` (schedule) + `[KEEP §3]` (hot-spots) + `[DROP]` (comp/agenda) | `SELECT scheduled_at FROM interview_events WHERE id=31` |

**Causal-proof status**: every row with a `kg://`/`db://`/`cd://` target had its verifiable query **executed read-only and confirmed passing** (ids 1/2/6/10/13/31 + nodes 251/252/255/257/261/297/298/300 all resolve). The `[DROP]` rows (Q6/P5/P6) carry **no unique preservable claim** -- they are ephemeral or derived. The `[OPEN-Q]` row (P1b verbatim self-intro script) and the `[KEEP]` rows (Q3b/P3/S2 hot-spots) are *not* dropped: P1b is surfaced for your call (§ Open Questions), KEEP rows survive inside the §3 skeleton. No row is silently dropped.

**Figure-framing caveat (the one thing to eyeball in §2)**: the doc's headline numbers "94% cost reduction", "90% latency reduction", and "+1% GMB first experiment" are **derived/rounded framings**. The canonical stories preserve the *underlying* facts (id=13: ~18K labels/day @ ~$500 vs $0.30-0.80/label; id=6: +0.6%/reuse, 200M+ total; id=2: the literal +1% diversity seed) but **not the computed percentages verbatim** -- read-only grep confirms "94%", "90%", and "latency" appear in zero behavioral_examples. A naive `grep "94%"` against the DB fails. This is preservation-of-substance, not preservation-of-string -- acceptable for archive, but flagged because it is precisely the kind of figure-drift a §2 review exists to catch. (If you want the exact recruiter-facing percentages retained as canonical, that is an argument for keeping AP as a short metrics block in the §3 skeleton -- a third open question.)

### Open questions for the user (per protocol §2 rule: no-target paragraphs are surfaced, not dropped)

- **OQ1 (P1b)**: The verbatim self-intro script and rehearsed Q-answers are personal/ephemeral and have no reusable KG home. Drop them, or keep the doc as a frozen historical artifact (read-only, no skeleton swap)? Given the call already happened 2026-04-15, recommendation = **drop** (the metrics live in `db://6`/`db://13`).
- **OQ2 (P3/Q3b)**: Slack-specific product context (Slack AI / Agentforce / Einstein surface map) is real knowledge but not de-companiable. Keep it as a prose block inside the thin skeleton (recommended), or move it to `node_company_tags` (S5) against the relevant SD nodes? S5 is empty today; the skeleton block is the lower-effort home.

---

## §3 Skeleton preview (replacement `company_documents.id=59` content)

The thin drawer-link doc that replaces the 13.8KB prep doc. Retains the one non-de-companiable block (Slack ML surface map) as compact prose; everything else becomes a cross-reference. Renders in `CompanyDrawer.Notes` (react-markdown + KaTeX) without markup errors.

```markdown
# Slack (Salesforce) Prep Index

Cross-references into the KG. Per-company specifics live in tags; shared
substrate lives in `meta-prep/*` nodes. Recruiter screen occurred 2026-04-15
(see Interview Timeline); this is the durable index that replaced the one-time
call-prep doc.

## Interview Pipeline
- [Pre-Loop: Recruiter Call + Tech Screen](kg://298) -- recruiter-screen mechanics, ~25% cut rate
- [Standard 4-Round MLE Virtual Onsite](kg://297) -- expected loop body
- Schedule: see Interview Timeline (HR call 2026-04-15, HM round 2026-04-22 / Scott Clark)

## Behavioral (my stories for this loop)
- [Allocation Framework as Reusable Platform Primitive](db://6) -- primary: marketplace multi-objective, +1% GMB
- [Cross-Org Boundary Defense via LLM Relevance Pipeline](db://13) -- backup: LLM-as-Judge eval, 94% cost / 90% latency
- [STARR Delivery Framework](kg://251) -- self-intro + story structure

## System Design substrate (relevant to Slack ranking/search/summarization)
- [Multi-Stage Retrieval-Ranking Funnel](kg://255) -- channel/mention/search rankers
- [Two-Tower / Dual-Encoder Retrieval](kg://252) -- enterprise search over private corpora
- [Learning-to-Rank: Pointwise/Pairwise/Listwise](kg://257)
- [Ranking Evaluation: NDCG / Recall@K / MRR](kg://261) -- + LLM-eval for summary/search-answer quality

## Slack-specific ML surface map (company flavor -- not in shared KG)
- **Slack AI** (paid): channel/thread summary, search answers (RAG over workspace msgs), recap. Pain: quality measurement, hallucination over private enterprise data, multi-tenancy isolation.
- **Message/channel ranking**: sidebar ordering, mentions, activity feed (personalization + engagement).
- **Enterprise search**: cross-surface (Messages/Files/People/Canvases), permission-aware indexing.
- **Agentforce integration**: Salesforce Einstein agents inside Slack surface; routing, tool use, grounding.
- **Platform context**: Einstein (Salesforce ML platform), Agentforce (2024 LLM-agent product line), Data Cloud (unified customer features).
```

(~1.4 KB skeleton vs 15.2 KB original -> ~91% byte reduction; the retained Slack-flavor block is ~0.7 KB.)

---

## §4 Hard-archive checklist

> **Recommendation**: because Slack is a single-doc, low-risk case and the §3 skeleton *replaces* (not deletes) the drawer doc, the apply is a **content-swap UPDATE**, not a row DELETE. The doc id (59) and its drawer presence are preserved; only the body changes. restore.sql still captures the pre-state for full rollback.

Apply seed: `scripts/_archive_slack_2026-06-18.py` (underscore = throwaway, one-shot). Sequence:

1. **DB backup**: copy `data/mle_prep.db` -> `data/mle_prep.db.bak.<timestamp>_pre_slack_archive`.
2. **Pre-write causal-proof gate**: run every §2 `可验证查询` with a non-`[DROP]`/`[OPEN-Q]` target; ABORT before any write if any returns empty (id 6/13/31/297/298/251/252/255/257/261 must all resolve).
3. **UPDATE (content-swap)**: `UPDATE company_documents SET content = <§3 skeleton>, content_hash = <sha256> WHERE id = 59` (sentinel idempotency: if current content already == skeleton, print `[SKIP]`).
4. **UPDATE clears (S2)**: `UPDATE companies SET notes = 'Slack ML team (Salesforce). See prep index doc + Interview Timeline.' WHERE id = 32` (drops the duplicated schedule/comp/agenda; the schedule already lives in `interview_events` id=31). `prep_notes` already NULL -- no change.
5. **Seed script move**: `git mv scripts/seed_slack_hr_call_prep.py archive/seed_scripts/2026-06-18/` (mandatory -- otherwise a re-run silently restores the 13.8KB body). **Leave** `scripts/_add_slack_hm_2026-04-22.py` in place (it seeds `interview_events`, the schedule we are preserving).
6. **restore.sql generation**: write `archive/company_internalized/slack_2026-06-18/restore.sql` with the full pre-state INSERT/UPDATE: original `company_documents.id=59` content (13 791 chars) + original `companies.notes` (372B). This is the rollback artifact.

Single transaction wraps steps 3-4. Verbose diff: print byte deltas (15 181 -> ~1 400), the skeleton sha256, and the row counts touched.

---

## §5 Promotion candidates flagged for meta-prep

Per `promotion_criteria.md`: threshold = appears in **>=3 of 11 P0+P1 companies** AND wording is de-companiable.

| Candidate | Companies w/ it (so far) | Proposed target | Threshold |
|---|---|---|---|
| **Recruiter-screen tactics** (comp-anchoring "ask band first"; positive why-leaving frame; red-flags-not-to-say; questions-to-ask template) | **Slack, Uber** = 2/11 | NEW `kg://meta-prep/onsite-loop-templates/recruiter-screen-tactics`, OR extend `kg://298` body | **BELOW (2 < 3) -- WATCH, do not promote** |

De-companied excerpt (for when threshold is met): *"On comp: never anchor first. Ask for the band, cite a peer-range you've seen ('Senior MLE TC $X-Y'), record their band without accept/reject. On why-leaving: positive frame only -- growth done on surface A, want wider surface B; never salary/manager/politics. Always prepare 3 questions: team structure, 6-12mo roadmap, exact loop shape."*

**Recommendation**: defer. Two companies is below threshold; promoting now risks Slack/Uber-flavored wording. Re-evaluate when Adobe (815) / LinkedIn (816) / DoorDash (819) B4a plans run -- if any of those S3 docs carry the same recruiter-tactic prose, the cluster crosses 3/11 and T-P1-821 (`B4-promotion`) authors the node. (Note: a LIKE-scan of all company_documents for "Why are you looking / Comp expectation / Tell me about yourself" today returns only Slack + Uber, so the other heavy docs are technical study notes, not recruiter prep -- the cluster may stay below threshold.)

---

## Next steps (post-review)

1. You review §2 + §5 + the two Open Questions (OQ1 verbatim-script drop; OQ2 Slack-flavor home).
2. On explicit "ok 执行", I author `scripts/_archive_slack_2026-06-18.py` per §4 (with the causal-proof gate + restore.sql) and run the §5/§6 protocol verification.
3. The recruiter-tactics promotion (§5) stays parked for T-P1-821 until the cluster crosses 3/11.

> Generated as the T-P1-818 dry-run. WROTE NOTHING TO DB -- this plan is the gate.
